@using EVotingSystem.Helpers;
@using Microsoft.AspNetCore.Html;
@using EVotingSystem.Utilities;
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@model CandidateModel[]
@{
    ViewData["Title"] = "Candidate Page";
}
<div class="jumbotron rounded-lg" style="min-height: 65vh">
    <div class="container-fluid">
        <div class="card-columns">
            @{ StudentModel Student = IdentityHandler.StudentSession(HttpContextAccessor.HttpContext.Request); }
            @foreach (CandidateModel CM in Model)
            {
                <form class="" asp-controller="Candidates" asp-action="Index" asp-route-id="@CM.Id" method="post">
                    <div class="card">
                       
                        @(new HtmlString(@CandidateHelper.GetCandidateHtmlImage(CM.Image)));
                        <div class="card-body">
                            <h6 class="card-title">@CM.Name</h6>
                            <h5 class="card-title">@CM.Id</h5>
                            <a class="btn btn-primary btn-block text-white" data-toggle="collapse" data-target="#@("Collapse" + CM.Id)" aria-expanded="false" aria-controls="@("Collapse" + CM.Id)">More Information</a>

                            @if (Student != null && Student.SentVotes != null && Student.SentVotes.Any(c => c.Id == CM.Id))
                            {
                                <button class="btn btn-danger btn-block mt-md-2" type="submit">
                                    Unvote
                                </button>
                            }
                            else
                            {
                                <button class="btn btn-primary btn-block mt-md-2" type="submit">
                                    Vote
                                </button>
                            }
                        </div>
                        <div class="card-footer">
                            <small class="text-muted">Last active vote @CM.LastVoteReceived</small>
                        </div>
                        <div class="collapse" id="@("Collapse" + CM.Id)">
                            <div class="card card-body">
                                <p>Total Votes: @CM.TotalVotes</p>
                                <p>Speach: @CM.Speach</p>
                            </div>
                        </div>
                    </div>
                </form>
            }
        </div>
    </div>
</div>
@*<form class="mt-md-5" asp-controller="Candidates" asp-action="Index" method="post">
        <div class="form-group form-row justify-content-md-center">
            <div class="col-md-4 mb-3">
                <label asp-for="Name" for="NameTextBox">Name</label>
                <input asp-for="Name" type="text" class="form-control" id="NameTextBox" placeholder="Enter your Name">
            </div>
            <div class="col-md-4 mb-3">
                <label asp-for="Gender" for="GenderTextBox">Gender</label>
                <input asp-for="Gender" type="text" class="form-control" id="GenderTextBox" placeholder="Enter your Gender">
            </div>
        </div>
        <div class="form-group form-row justify-content-md-center">
            <div class="col-md-4 mb-3">
                <label asp-for="Id" for="IdTextBox">Id</label>
                <input asp-for="Id" type="text" class="form-control" id="IdTextBox" placeholder="Enter your Id">
            </div>
            <div class="col-md-4 mb-3">
                <label asp-for="Speach" for="SpeachTextBox">Speach</label>
                <textarea asp-for="Speach" type="text" class="form-control" id="SpeachTextBox" placeholder="Enter your Speach"></textarea>
            </div>
        </div>
        <div class="form-group form-row justify-content-md-center">
            <div class="col-md-4 mb-3">
                <label asp-for="Image" for="ImageTextBox">Image</label>
                <textarea asp-for="Image" type="text" class="form-control" id="ImageTextBox" placeholder="Enter your Image"></textarea>
            </div>
        </div>
        <div class="form-row justify-content-center">
            <div class="col-md-4 mb-3">
                <button id="AddCandidateButton" type="submit" class="btn btn-primary btn-lg btn-block d-block">
                    Add Candidate
                </button>
            </div>
        </div>
    </form>*@